import icon from '../assets/empty-cart.png'

const CartWidget = () => {
    return (
        <div className="header--menu--icono">
            <img src={icon}></img>

        </div>
    )
}

export default CartWidget